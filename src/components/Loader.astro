---
interface Props {
    class?: string;
}

const { class: className } = Astro.props as Props;
---

<span
    class:list={[
        "size-8 animate-spin rounded-full border-4 border-main/20 border-t-main",
        className,
    ]}
    aria-hidden="true"
></span>
