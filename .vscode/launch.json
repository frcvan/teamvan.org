{
    "version": "0.2.0",
    "configurations": [
        {
            "command": "pnpm exec astro dev --background --open",
            "name": "Preview website locally",
            "request": "launch",
            "type": "node-terminal"
        }
    ]
}
